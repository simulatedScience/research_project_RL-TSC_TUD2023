includes:
  - configs/tsc/base.yml

world:
  gui: True # redundant

model:
  name: presslight
  train_model: True
  test_model: False
  load_model: False
  d_dense: 128
  epsilon: 0.1
  epsilon_decay: 0.995
  epsilon_min: 0.01
  one_hot: True
  phase: True

trainer:
  learning_start: 1000
  buffer_size: 5000
